using System;
using System.Linq;
using UnityEngine;

public class ResistanceComponent : MonoBehaviour
{
    //[SerializeField] DamageRestiance[] _resistances;

    //public DamageRestiance GetMultiplier(ElementType type) => _resistances.FirstOrDefault(item => item.Type == type);
}
